import { makeStyles } from '@material-ui/styles';

export default makeStyles({
  cardContainer: {
    marginBottom: '8px',
  },
  poster: {
    width: '120px',
  },
  titleContainer: {
    paddingLeft: 8,
  },
});
